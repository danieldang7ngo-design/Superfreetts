[pytest]
testpaths = tests
pythonpath = .
norecursedirs = external .git __pycache__
addopts = -v
markers =
    unit: Unit tests (no external deps)
    integration: Integration tests (may use filesystem/threads)
    service: Service tests
